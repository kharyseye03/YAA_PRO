<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12 et plus.
     Depuis cette version, le système impose son propre écran de
     lancement et ignore `windowBackground` : il affiche l'icône de
     l'app sur un fond qu'on ne contrôle que par ces deux attributs.
     Sans eux, le fond retombe sur celui du thème système — noir sur un
     téléphone en mode sombre, d'où l'écran noir au démarrage. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
